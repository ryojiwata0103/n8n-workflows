{
  "mcpServers": {
    "n8n-mcp": {
      "command": "npx",
      "args": [
        "-y",
        "n8n-mcp@latest"
      ],
      "env": {
        "MCP_MODE": "stdio",
        "MCP_LOG_LEVEL": "info",
        "NODE_ENV": "production",
        "REBUILD_ON_START": "false"
      }
    }
  }
}